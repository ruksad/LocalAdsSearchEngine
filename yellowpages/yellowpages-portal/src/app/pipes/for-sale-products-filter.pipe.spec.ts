import { ForSaleProductsFilterPipe } from './for-sale-products-filter.pipe';

describe('ForSaleProductsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ForSaleProductsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
