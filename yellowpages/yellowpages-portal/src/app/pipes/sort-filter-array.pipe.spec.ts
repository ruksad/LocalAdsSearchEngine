import { SortFilterArrayPipe } from './sort-filter-array.pipe';

describe('SortFilterArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new SortFilterArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
